name: Auto_Post_Stray_cat_1

on:
  schedule:
    # 1. รอบเช้า (เป้า 08:00 ไทย) -> ตื่น 07:00 ไทย
    # สูตร: 07:00 - 7 = 00:00 UTC
    - cron: '0 0 * * *'
    
    # 2. รอบบ่าย (เป้า 12:00 ไทย) -> ตื่น 11:00 ไทย
    # สูตร: 11:00 - 7 = 04:00 UTC
    - cron: '0 4 * * *'
    
    # 3. รอบเย็น (เป้า 17:00 ไทย) -> ตื่น 16:00 ไทย
    # สูตร: 16:00 - 7 = 09:00 UTC
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'
          
      - run: pip install tweepy
      
      - name: Run Stray Cat 1 Script
        env:
          PYTHONUNBUFFERED: 1
          # ใช้ Secret ชุด SC1 (Stray Cat 1)
          CONSUMER_KEY: ${{ secrets.SC1_CONSUMER_KEY }}
          CONSUMER_SECRET: ${{ secrets.SC1_CONSUMER_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.SC1_X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.SC1_X_ACCESS_TOKEN_SECRET }}
        run: python Auto_Post_Stray_cat_1.py
